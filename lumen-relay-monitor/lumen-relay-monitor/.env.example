# === LUMEN RELAY + MONITOR (Base Mainnet) ===
# Required
KERNEL_ADDRESS=0x52078D914CbccD78EE856b37b438818afaB3899c

# One of these is enough. WS is recommended (real-time). If WS is missing, relay falls back to HTTP log polling.
BASE_WS_URL=wss://mainnet.base.org
BASE_RPC_URL=https://mainnet.base.org

# Optional
PORT=8787
DB_PATH=./lumen.db
# If first run and you want controlled backfill, set one:
# START_BLOCK=0
BACKFILL_BLOCKS=5000
POLL_INTERVAL_MS=2000

# Optional (only if you want /emit to publish test contexts from the server)
# PRIVATE_KEY=0x...
